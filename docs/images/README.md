# Images du portfolio

- `demo.svg` — illustration synthetique animee (tags en mouvement, alerte
  zone). Ce n'est PAS une capture de l'app reelle, juste un placeholder qui
  bouge dans le README de GitHub/GitLab.
- `demo.gif` *(a produire)* — capture reelle du client MAUI, 5 a 8 secondes.
  Outil recommande : ScreenToGif (Windows, portable, gratuit). Cibler
  moins de 5 Mo. Une fois present, remplacer la ligne `demo.svg` du README
  racine par `demo.gif`.
